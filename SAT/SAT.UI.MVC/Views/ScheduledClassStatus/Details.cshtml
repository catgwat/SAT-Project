@model SAT.DATA.EF.Models.ScheduledClassStatus

@{
    ViewData["Title"] = Model.ScheduledClasses + " Details";
}

<div class="choose_section layout_padding">
    <div class="container text-center">
        <h1 class="h1">@Model.Scsname Details</h1>
    </div>
</div>

<div class="row mb-3">
    @* <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Scsname)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Scsname)
        </dd>
    </dl> *@

    <div class="col-md-4 offset-md-4">
        <p class="h3">
            <strong>@Html.DisplayNameFor(model => model.Scsname):</strong>
            @Html.DisplayFor(model => model.Scsname)
        </p>
        <p class="h3 mt-5">
            <a class="btn btn-info" asp-action="Edit" asp-route-id="@Model.Scsid">Edit</a>
            <a class="btn btn-secondary" asp-action="Index">Back to Student Statuses</a>
        </p>
    </div>
</div>
